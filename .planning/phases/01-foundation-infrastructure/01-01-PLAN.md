---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - .env.local.example
  - .gitignore
  - .nvmrc
autonomous: true

must_haves:
  truths:
    - "Developer can run `npm run dev` and see a working Next.js app at localhost:3000"
    - "TypeScript compilation succeeds with zero errors"
    - "Tailwind CSS v4 classes render correctly in the browser"
    - "Project has staging branch created alongside main"
  artifacts:
    - path: "package.json"
      provides: "Project dependencies and scripts"
      contains: "next"
    - path: "next.config.ts"
      provides: "Next.js 16 configuration with image remotePatterns for Sanity CDN"
      exports: ["default"]
    - path: "app/globals.css"
      provides: "Tailwind v4 CSS-first imports"
      contains: "@import \"tailwindcss\""
    - path: "app/layout.tsx"
      provides: "Root layout with metadata"
      exports: ["default", "metadata"]
    - path: ".env.local.example"
      provides: "Template for all required environment variables"
  key_links:
    - from: "app/globals.css"
      to: "postcss.config.mjs"
      via: "Tailwind v4 PostCSS plugin"
      pattern: "@tailwindcss/postcss"
    - from: "app/layout.tsx"
      to: "app/globals.css"
      via: "CSS import"
      pattern: "import.*globals.css"
---

<objective>
Initialize Next.js 16 project with TypeScript, Tailwind CSS v4, and development environment scaffolding.

Purpose: Establish the foundational codebase that all subsequent plans build upon.
Output: Working Next.js 16 app with Tailwind v4, proper project structure, and staging branch.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 16 project with TypeScript and Tailwind v4</name>
  <files>
    package.json
    tsconfig.json
    next.config.ts
    postcss.config.mjs
    app/layout.tsx
    app/page.tsx
    app/globals.css
    .gitignore
    .nvmrc
  </files>
  <action>
Run `npx create-next-app@latest . --typescript --eslint --tailwind --app --turbopack --import-alias "@/*"` in the project root (note: project root is `/Users/mac/Documents/Code/C&C/` which already has .git and .planning -- the command should work in existing directory).

After scaffolding:

1. Verify `postcss.config.mjs` uses `@tailwindcss/postcss` plugin (Tailwind v4 CSS-first config). If not, update it:
```javascript
const config = { plugins: { "@tailwindcss/postcss": {} } };
export default config;
```

2. Update `app/globals.css` to use Tailwind v4 import:
```css
@import "tailwindcss";
```

3. Update `next.config.ts` to include Sanity CDN remote patterns and AVIF/WebP image formats:
```typescript
const nextConfig = {
  images: {
    remotePatterns: [
      { protocol: 'https' as const, hostname: 'cdn.sanity.io' },
    ],
    formats: ['image/avif', 'image/webp'] as const,
  },
};
export default nextConfig;
```

4. Update `app/layout.tsx` with proper metadata for Checkmate & Connect:
- Title template: `%s | Checkmate & Connect`
- Default title: `Checkmate & Connect - Chess & Entrepreneurship Community`
- Description: Community in Casablanca, Morocco
- OpenGraph basics

5. Replace `app/page.tsx` with a simple placeholder page that uses Tailwind classes (confirms styling works).

6. Create `.nvmrc` with `20` (Node 20 for Netlify compatibility).

7. Create `.env.local.example` with all env var placeholders (no real values):
```
# Sanity
NEXT_PUBLIC_SANITY_PROJECT_ID=
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-01
SANITY_REVALIDATE_SECRET=

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# reCAPTCHA
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=
RECAPTCHA_SECRET_KEY=
```

8. Ensure `.gitignore` includes `.env.local` (create-next-app should handle this).
  </action>
  <verify>
Run `npm run dev` and confirm the app starts on localhost:3000 without errors. Run `npx tsc --noEmit` to verify TypeScript compiles cleanly. Visually confirm Tailwind classes render (check page source or dev tools for generated CSS).
  </verify>
  <done>
Next.js 16 app runs locally with TypeScript and Tailwind v4 rendering correctly. next.config.ts has Sanity CDN image patterns. .env.local.example documents all required env vars.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create project directory structure and staging branch</name>
  <files>
    lib/sanity/client.ts
    lib/sanity/queries.ts
    lib/sanity/imageUrl.ts
    lib/sanity/schemas/index.ts
    lib/supabase/client.ts
    lib/supabase/server.ts
    components/.gitkeep
    app/(public)/layout.tsx
    app/(admin)/layout.tsx
  </files>
  <action>
Create the directory structure from research (skeleton files only -- actual implementations come in Plans 02 and 03):

1. `lib/sanity/client.ts` -- export placeholder Sanity client (comment: "Configured in Plan 02")
2. `lib/sanity/queries.ts` -- empty file with comment about GROQ queries
3. `lib/sanity/imageUrl.ts` -- empty file with comment about image URL builder
4. `lib/sanity/schemas/index.ts` -- empty export array
5. `lib/supabase/client.ts` -- export placeholder browser client (comment: "Configured in Plan 03")
6. `lib/supabase/server.ts` -- export placeholder server client (comment: "Configured in Plan 03")
7. `components/.gitkeep` -- empty file to track directory
8. `app/(public)/layout.tsx` -- simple pass-through layout for public routes (just renders children)
9. `app/(admin)/layout.tsx` -- simple pass-through layout for admin routes (just renders children, auth added in Phase 4)

Each placeholder file should be a valid TypeScript file that exports something (even if just a comment and empty export) so imports don't break.

Then create the staging branch:
```bash
git checkout -b staging
git checkout main
```

This establishes the branching strategy: main (production) and staging (preview deploys).
  </action>
  <verify>
Run `npm run dev` to confirm no import errors from placeholder files. Run `git branch` to confirm both `main` and `staging` branches exist. Run `npx tsc --noEmit` to confirm TypeScript still compiles.
  </verify>
  <done>
Project directory structure matches research architecture. Placeholder files exist for Sanity and Supabase libs. Both main and staging git branches exist. TypeScript compiles cleanly.
  </done>
</task>

</tasks>

<verification>
- `npm run dev` starts without errors
- `npx tsc --noEmit` passes
- `git branch` shows main and staging
- `.env.local.example` lists all required env vars
- Tailwind classes render on the placeholder page
</verification>

<success_criteria>
Developer can clone repo, copy .env.local.example to .env.local, run `npm install && npm run dev`, and see a working Next.js 16 app with Tailwind v4 styling at localhost:3000. Project structure is ready for Sanity and Supabase integration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md`
</output>
