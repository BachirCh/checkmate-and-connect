---
phase: 07-add-blog-functionality
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(public)/blog/page.tsx
  - app/(public)/blog/[slug]/page.tsx
  - components/BlogCard.tsx
  - components/BlogPostContent.tsx
  - lib/sanity/portableTextComponents.tsx
  - lib/sanity/queries.ts
autonomous: true

must_haves:
  truths:
    - "Visitor can view blog listing page with all published posts in reverse chronological order"
    - "Visitor can click a blog post to read the full content"
    - "Blog post content renders rich text with images, headings, lists, and links"
    - "Blog posts without publishedAt date do not appear on public pages"
  artifacts:
    - path: "app/(public)/blog/page.tsx"
      provides: "Blog listing page with published posts"
      min_lines: 60
      exports: ["default", "metadata"]
    - path: "app/(public)/blog/[slug]/page.tsx"
      provides: "Blog post detail page with SEO metadata"
      min_lines: 80
      exports: ["default", "generateMetadata", "generateStaticParams"]
    - path: "components/BlogCard.tsx"
      provides: "Blog post card for listing"
      min_lines: 40
    - path: "components/BlogPostContent.tsx"
      provides: "Portable Text renderer for blog body"
      min_lines: 50
    - path: "lib/sanity/portableTextComponents.tsx"
      provides: "Custom Portable Text components"
      min_lines: 60
  key_links:
    - from: "app/(public)/blog/page.tsx"
      to: "lib/sanity/queries.ts"
      via: "imports blogPostsQuery"
      pattern: "import.*blogPostsQuery"
    - from: "app/(public)/blog/[slug]/page.tsx"
      to: "lib/sanity/queries.ts"
      via: "imports blogPostBySlugQuery"
      pattern: "import.*blogPostBySlugQuery"
    - from: "components/BlogPostContent.tsx"
      to: "lib/sanity/portableTextComponents.tsx"
      via: "imports portableTextComponents"
      pattern: "import.*portableTextComponents"
    - from: "lib/sanity/portableTextComponents.tsx"
      to: "@portabletext/react"
      via: "uses PortableTextComponents type"
      pattern: "PortableTextComponents"
---

<objective>
Build public-facing blog pages where visitors can browse published blog posts in reverse chronological order and read individual posts with rich Portable Text content rendering.

Purpose: Enable content publishing for community updates, meetup recaps, and key takeaways using the existing blog post schema from Phase 1.

Output: Blog listing page at `/blog`, individual blog post pages at `/blog/[slug]`, and Portable Text rendering components.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-add-blog-functionality/07-RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md
@.planning/phases/05-member-directory-management/05-01-SUMMARY.md

# Existing files to reference
@lib/sanity/schemas/blogPost.ts
@lib/sanity/queries.ts
@lib/sanity/client.ts
@lib/sanity/imageUrl.ts
@components/MemberCard.tsx
@app/(public)/members/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create blog listing page with published posts filter</name>
  <files>
    app/(public)/blog/page.tsx
    components/BlogCard.tsx
    lib/sanity/queries.ts
  </files>
  <action>
Create blog listing page following the existing member directory pattern from 05-01-PLAN.md.

**app/(public)/blog/page.tsx:**
- Use async Server Component for data fetching (no loading states)
- Import `client` from `@/lib/sanity/client`
- Update `blogPostsQuery` in lib/sanity/queries.ts to filter by `defined(publishedAt)` to exclude draft posts
- Fetch blog posts using updated query: `const posts = await client.fetch(blogPostsQuery)`
- Export metadata with title "Blog | Checkmate & Connect" and description about meetups and community highlights
- Display heading "Blog" with post count (e.g., "12 posts" or "No posts yet. Check back soon!")
- Render responsive grid using same pattern as member directory: `grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6`
- Map over posts and render BlogCard for each
- Show empty state if no published posts exist
- Use black background, white text theme (consistent with existing design)

**components/BlogCard.tsx:**
- Accept `post` prop with type `{ _id: string; title: string; slug: { current: string }; coverImage: any; excerpt: string; publishedAt: string; author: string }`
- Import `urlFor` from `@/lib/sanity/imageUrl`
- Optimize cover image with `.width(800).height(450).fit('crop').auto('format')` (16:9 aspect ratio)
- Render Link to `/blog/${post.slug.current}` wrapping entire card
- Show cover image with aspect-video (16:9)
- Display title (text-2xl, font-bold)
- Show excerpt if exists (text-gray-400, truncate to 2 lines)
- Display author and formatted date (e.g., "February 17, 2026") using `new Date(publishedAt).toLocaleDateString()`
- Use hover effect: scale-105 transition on card
- Style with black background, white text, gray-800 border

**lib/sanity/queries.ts:**
- Update `blogPostsQuery` to add `&& defined(publishedAt)` filter after `_type == "blogPost"`
- This ensures only posts with a publish date appear (drafts remain hidden)
- Keep existing field projection and `order(publishedAt desc)`
</action>
  <verify>
1. Visit http://localhost:3000/blog - page loads without errors
2. Check page metadata in browser DevTools (title should be "Blog | Checkmate & Connect")
3. Verify GROQ query includes `defined(publishedAt)` filter in lib/sanity/queries.ts
4. If no published posts exist, empty state message appears
5. If posts exist, grid layout displays with responsive columns (1/2/3)
6. Each BlogCard shows cover image, title, excerpt, author, and date
7. Clicking a BlogCard navigates to `/blog/[slug]` (will 404 until Task 2)
  </verify>
  <done>
Blog listing page displays all published blog posts in reverse chronological order with responsive grid layout and empty state handling. Draft posts (no publishedAt) do not appear.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create blog post detail page with dynamic routing and SEO metadata</name>
  <files>
    app/(public)/blog/[slug]/page.tsx
  </files>
  <action>
Create individual blog post page using Next.js 16 dynamic routes with generateStaticParams for ISR.

**app/(public)/blog/[slug]/page.tsx:**
- Import `client` from `@/lib/sanity/client`
- Import `blogPostBySlugQuery` from `@/lib/sanity/queries`
- Import `urlFor` from `@/lib/sanity/imageUrl`
- Import `notFound` from `next/navigation`
- Import BlogPostContent component (will be created in Task 3)

**Create type for Props:**
```typescript
type Props = {
  params: Promise<{ slug: string }>;
};
```

**Export generateStaticParams function:**
- Fetch all blog post slugs: `*[_type == "blogPost" && defined(slug.current)] { "slug": slug.current }`
- Map to array of `{ slug: string }` objects
- This enables static generation at build time for all published posts

**Export generateMetadata function:**
- Accept `{ params }: Props` and return `Promise<Metadata>`
- Await params to get slug: `const { slug } = await params`
- Fetch post using `blogPostBySlugQuery` with `{ slug }` parameter
- Return 404 metadata if post not found
- Generate Open Graph image URL using `urlFor(coverImage).width(1200).height(630).url()` (1200x630 per OG spec)
- Return metadata object with:
  - title: `${post.title} | Checkmate & Connect`
  - description: post.excerpt or fallback
  - openGraph: title, description, type: 'article', publishedTime, authors: [author], images: [ogImage]
  - twitter: card: 'summary_large_image', title, description, images: [ogImage]

**Default page component:**
- Accept `{ params }: Props`
- Await params to get slug
- Fetch full post including body field using `blogPostBySlugQuery`
- Call `notFound()` if post doesn't exist
- Render main with black background, white text
- Create article wrapper with max-w-3xl, centered, padding
- Render BlogPostContent component passing post prop
- BlogPostContent will handle cover image, header, and body rendering (Task 3)
</action>
  <verify>
1. TypeScript compilation succeeds with no errors
2. Visit http://localhost:3000/blog/[any-slug] - returns 404 if slug doesn't exist (expected)
3. If a published post exists in Sanity, visiting its slug shows the page (body won't render until Task 3)
4. Check page source - Open Graph meta tags present with correct og:image dimensions (1200x630)
5. Check page title in browser tab - shows "Post Title | Checkmate & Connect"
6. generateStaticParams exports successfully (no TS errors)
  </verify>
  <done>
Individual blog post pages render with dynamic routing, SEO-optimized metadata including Open Graph tags, and 404 handling for non-existent posts. Static params generation enables ISR.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement Portable Text rendering with custom components</name>
  <files>
    lib/sanity/portableTextComponents.tsx
    components/BlogPostContent.tsx
  </files>
  <action>
Create Portable Text renderer for secure, styled blog content display using @portabletext/react.

**lib/sanity/portableTextComponents.tsx:**
- Import `PortableTextComponents` from `@portabletext/react`
- Import `urlFor` from `@/lib/sanity/imageUrl`
- Export `portableTextComponents` object of type `PortableTextComponents`

**Configure custom components:**
- `types.image`: Render images from Portable Text body
  - Check if `value?.asset` exists, return null if not
  - Generate image URL: `urlFor(value).width(800).auto('format').url()`
  - Render figure with img tag (width: 100%, rounded-lg)
  - Show figcaption if `value.caption` exists (text-center, text-gray-400, text-sm)

- `marks.link`: Render hyperlinks with security attributes
  - Detect external links (not starting with '/')
  - Add `rel="noopener noreferrer"` and `target="_blank"` for external links
  - Style with `text-blue-400 hover:underline`

- `block.h2`: Render h2 headings with `text-3xl font-bold mt-8 mb-4`
- `block.h3`: Render h3 headings with `text-2xl font-bold mt-6 mb-3`
- `block.normal`: Render paragraphs with `text-gray-300 leading-relaxed mb-4`

- `list.bullet`: Render ul with `list-disc list-inside space-y-2 mb-4 text-gray-300`
- `list.number`: Render ol with `list-decimal list-inside space-y-2 mb-4 text-gray-300`

**components/BlogPostContent.tsx:**
- Mark with `'use client'` directive (PortableText requires client-side rendering)
- Import `PortableText` from `@portabletext/react`
- Import `portableTextComponents` from `@/lib/sanity/portableTextComponents`
- Import `urlFor` from `@/lib/sanity/imageUrl`

**Create BlogPostContentProps type:**
```typescript
type BlogPostContentProps = {
  post: {
    title: string;
    coverImage: any;
    publishedAt: string;
    author: string;
    excerpt?: string;
    body: any[];
  };
};
```

**Component structure:**
1. Generate cover image URL if exists: `urlFor(coverImage).width(1200).height(600).fit('crop').auto('format').url()`
2. Render cover image (full width, rounded-lg, mb-8) if coverImageUrl exists
3. Render header section with:
   - Post title (text-4xl sm:text-5xl, font-bold, mb-4)
   - Metadata row with author and formatted date (text-gray-400, text-sm)
   - Format date: `new Date(publishedAt).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })`
4. Render body with prose classes for typography:
   - Wrapper div with `prose prose-invert prose-lg max-w-none`
   - `<PortableText value={post.body} components={portableTextComponents} />`

IMPORTANT: Define `portableTextComponents` outside component or use React.useMemo to avoid recreating on every render (performance optimization per @portabletext/react docs).
</action>
  <verify>
1. TypeScript compilation succeeds with no errors
2. Visit http://localhost:3000/blog/[slug] for a published post - full content renders
3. Check that images in Portable Text body render with Sanity CDN optimization (auto=format in URL)
4. Verify heading styles (h2, h3) appear correctly styled
5. Check that links open in new tab if external (rel="noopener noreferrer")
6. Verify lists (bullet and numbered) render with proper styling
7. Cover image displays at top with correct aspect ratio
8. Post header shows title, author, and formatted date
9. No XSS vulnerabilities (PortableText handles escaping automatically)
  </verify>
  <done>
Blog post body content renders securely using Portable Text with custom components for images, links, headings, and lists. Cover image and post header display with proper styling and metadata.
  </done>
</task>

</tasks>

<verification>
**Overall phase checks:**

1. **Blog listing page completeness:**
   - Visit http://localhost:3000/blog
   - Page loads with metadata (check <title> tag)
   - Shows post count or empty state message
   - Grid layout responsive (test browser resize)
   - BlogCard components show cover images, titles, excerpts, authors, dates

2. **Blog post detail page functionality:**
   - Click a blog post from listing
   - Individual post page loads at `/blog/[slug]`
   - Cover image displays at top
   - Title and metadata render correctly
   - Body content shows with proper typography
   - Images in body render from Sanity CDN
   - Links work and external links open in new tab
   - Headings, lists styled correctly

3. **Privacy compliance:**
   - Only posts with `publishedAt` date appear on listing
   - Draft posts (no publishedAt) do not appear
   - Check GROQ query in lib/sanity/queries.ts includes `defined(publishedAt)`

4. **SEO verification:**
   - View page source for `/blog/[slug]`
   - Verify Open Graph tags exist (og:title, og:description, og:image, og:type)
   - Verify Twitter Card tags exist
   - Check og:image URL has 1200x630 dimensions

5. **Image optimization:**
   - Check Network tab in DevTools
   - Verify images load from cdn.sanity.io
   - Verify URLs include `auto=format` parameter
   - Modern browsers should receive WebP/AVIF format

Run: `npm run build` - build should succeed with no errors
</verification>

<success_criteria>
- Visitor can view blog listing page at `/blog` with all published posts in reverse chronological order (BLOG-03)
- Visitor can read individual blog posts at `/blog/[slug]` with title, photos, and rich content (BLOG-02, BLOG-04)
- Blog posts include title, cover image, author, date, and body with Portable Text rendering (BLOG-02)
- Only posts with publishedAt date appear on public pages (draft protection)
- Blog post body supports inline images, headings, lists, and links with proper styling
- Images use Sanity CDN optimization (auto-format, hotspot cropping)
- SEO metadata configured with Open Graph tags for social sharing (BLOG-05 partial - tags added, sharing buttons in next plan)
- External links in blog content open in new tab with security attributes
- Responsive design works on mobile, tablet, and desktop
</success_criteria>

<output>
After completion, create `.planning/phases/07-add-blog-functionality/07-01-SUMMARY.md` following the template at `/Users/mac/.claude/get-shit-done/templates/summary.md`.

Include:
- Performance metrics (duration, tasks completed)
- Files created/modified
- Verification results
- Any deviations from plan
- Git commit SHAs for task commits
</output>
