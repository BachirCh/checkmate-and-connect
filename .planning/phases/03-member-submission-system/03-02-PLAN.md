---
phase: 03-member-submission-system
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/(public)/join/confirmation/page.tsx
autonomous: false

must_haves:
  truths:
    - "Visitor sees confirmation message after successful form submission"
    - "Confirmation page explains what happens next (review timeline, approval process)"
    - "Confirmation page provides navigation back to homepage"
  artifacts:
    - path: "app/(public)/join/confirmation/page.tsx"
      provides: "Success confirmation page with next steps"
      contains: "submitted"
  key_links:
    - from: "app/(public)/join/actions.ts"
      to: "app/(public)/join/confirmation/page.tsx"
      via: "redirect('/join/confirmation')"
      pattern: "redirect.*join/confirmation"
---

<objective>
Create the submission confirmation page and verify the complete member submission flow end-to-end.

Purpose: Complete the user journey by showing a clear confirmation after form submission, and verify the entire flow works correctly.
Output: Confirmation page at /join/confirmation and verified end-to-end submission flow.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-member-submission-system/03-01-SUMMARY.md
@app/(public)/join/actions.ts
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create confirmation page</name>
  <files>app/(public)/join/confirmation/page.tsx</files>
  <action>
Create confirmation page at `app/(public)/join/confirmation/page.tsx`:

- Server component (no 'use client' needed)
- Export metadata: title "Application Submitted | Checkmate & Connect"
- Visual success indicator: checkmark icon (inline SVG, green accent on dark background)
- Main heading: "Application Submitted!"
- Confirmation text: "Thank you for applying to join the Checkmate & Connect member directory."
- "What happens next?" section with numbered steps:
  1. "Our team will review your submission"
  2. "Once approved, your profile will appear in the member directory"
  3. "Connect with other members at our weekly Wednesday meetups"
- Call-to-action: Link back to homepage ("Return to Homepage")
- **Dark theme styling** consistent with existing site: black bg, white text, gray-800 borders
- The "what happens next" box should use a subtle border (gray-800) instead of blue-50 bg (which doesn't match dark theme)
- Responsive layout: max-w-2xl, centered, px-4, py-12
  </action>
  <verify>
- Visit `http://localhost:3000/join/confirmation` — page renders with checkmark, heading, next steps, and homepage link
- Styling matches dark theme (black bg, white text)
- `npx tsc --noEmit` passes
  </verify>
  <done>Confirmation page shows success message, explains review process with numbered steps, and provides clear navigation back to homepage, all styled consistently with the dark theme.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete member submission flow</name>
  <what-built>Complete member submission system: form at /join with validation, spam protection, Sanity CMS integration, photo upload, and confirmation at /join/confirmation</what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Visit http://localhost:3000/join
3. Verify form renders with: Name*, Job Title*, Company, LinkedIn URL, Profile Photo* fields
4. Try submitting empty form — should show validation errors for required fields
5. Fill in Name and Job Title, select a photo — verify image preview appears
6. Submit the form (Note: requires SANITY_WRITE_TOKEN in .env.local and RECAPTCHA keys configured)
7. If submission succeeds, verify redirect to /join/confirmation with success message
8. Check Sanity Studio at /studio — new member document should exist with status: "pending"
9. Check mobile responsiveness: resize browser to 375px width, verify form is usable with large touch targets
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `/join/confirmation` page renders with success message and next steps
2. Complete flow: fill form → submit → redirects to confirmation
3. New member appears in Sanity with status: pending
4. Form is mobile-responsive
5. Required field validation works on client side
</verification>

<success_criteria>
- Confirmation page at /join/confirmation shows success message with next steps
- Full submission flow works: form → validate → spam check → upload → create → confirm
- Member created in Sanity CMS with status: pending
- User-verified visual and functional correctness
</success_criteria>

<output>
After completion, create `.planning/phases/03-member-submission-system/03-02-SUMMARY.md`
</output>
