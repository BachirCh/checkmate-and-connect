---
phase: 02-landing-information
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - components/Hero.tsx
  - components/EventDetails.tsx
  - components/CommunityStats.tsx
  - components/Footer.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor can see what C&C is (chess + entrepreneurship community)"
    - "Visitor can see when meetups happen (every Wednesday at 6pm)"
    - "Visitor can see where meetups happen (Commons, Casablanca, Morocco)"
    - "Landing page has proper SEO metadata and Open Graph tags"
    - "Landing page has JSON-LD structured data for the recurring event"
    - "Page is mobile-first responsive"
  artifacts:
    - path: "app/layout.tsx"
      provides: "Root layout with optimized font, enhanced metadata"
      contains: "next/font"
    - path: "app/page.tsx"
      provides: "Landing page composing all sections with JSON-LD"
      contains: "application/ld+json"
    - path: "components/Hero.tsx"
      provides: "Hero section with community identity"
      contains: "Checkmate"
    - path: "components/EventDetails.tsx"
      provides: "When/where/who information cards"
      contains: "Wednesday"
    - path: "components/Footer.tsx"
      provides: "Site footer with community links"
  key_links:
    - from: "app/page.tsx"
      to: "components/Hero.tsx"
      via: "import"
      pattern: "import.*Hero"
    - from: "app/page.tsx"
      to: "components/EventDetails.tsx"
      via: "import"
      pattern: "import.*EventDetails"
---

<objective>
Build the landing page with hero section, event details (when/where/who), community stats, footer, SEO metadata, font optimization, and JSON-LD structured data for the recurring weekly meetup.

Purpose: This is the core "send someone a link" experience. When someone asks "what's C&C?", this page answers it immediately with community identity, meeting details, and social proof.
Output: Complete landing page with all informational sections, SEO-optimized, mobile-first responsive.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-information/02-RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md
@.planning/phases/01-foundation-infrastructure/01-02-SUMMARY.md
@app/layout.tsx
@app/page.tsx
@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance root layout with font optimization and full SEO metadata</name>
  <files>app/layout.tsx, app/globals.css</files>
  <action>
Update app/layout.tsx:
- Import Inter font from next/font/google with latin subset, swap display, and CSS variable --font-inter
- Apply font variable class to html element and font-sans to body
- Add antialiased class to body for crisp text rendering
- Enhance metadata export with:
  - metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL || 'https://checkmateconnect.com')
  - title template: '%s | Checkmate & Connect' (keep existing)
  - Full description: 'Join 200+ members every Wednesday at 6pm at Commons in Casablanca for chess and entrepreneurship meetups.'
  - keywords: ['chess', 'entrepreneurship', 'Casablanca', 'community', 'networking', 'Morocco', 'Commons']
  - openGraph with locale 'en_US', siteName 'Checkmate & Connect', images placeholder '/og-image.png' (1200x630)
  - twitter card: summary_large_image
  - robots: { index: true, follow: true }

Update app/globals.css:
- Keep @import "tailwindcss"
- Add @theme block to define --font-sans as var(--font-inter) for Tailwind v4 font integration
- Add smooth scroll behavior on html
- Add base body styles: bg-white text-gray-900

Do NOT use generateMetadata function — static metadata is sufficient since landing page content is hardcoded.
  </action>
  <verify>Run `npx tsc --noEmit` to confirm no TypeScript errors. Check that layout.tsx exports metadata with openGraph, twitter, and robots fields.</verify>
  <done>Root layout has Inter font with zero layout shift, comprehensive SEO metadata including Open Graph and Twitter cards, and global CSS theme configured for Tailwind v4.</done>
</task>

<task type="auto">
  <name>Task 2: Create landing page sections — Hero, EventDetails, CommunityStats, Footer</name>
  <files>components/Hero.tsx, components/EventDetails.tsx, components/CommunityStats.tsx, components/Footer.tsx</files>
  <action>
All components are Server Components (no 'use client'). Mobile-first responsive with Tailwind.

**components/Hero.tsx:**
- Full-width hero section with gradient background (chess-themed dark gradient, e.g., from-gray-900 to-blue-900)
- Large heading: "Checkmate & Connect" with responsive text sizing (text-4xl sm:text-5xl lg:text-6xl)
- Subheading: "Chess & Entrepreneurship Community"
- Brief tagline: "Join 200+ members every Wednesday in Casablanca"
- CTA button linking to the Meetup group (or anchor to event section): "Join Our Next Meetup" styled as prominent button
- Use px-4 py-16 sm:py-24 lg:py-32 for responsive vertical spacing
- Center-aligned text, white text on dark background

**components/EventDetails.tsx:**
- Section heading: "When & Where" or "Join Us"
- Grid layout: single column on mobile, 3 columns on lg (grid gap-6 md:grid-cols-2 lg:grid-cols-3)
- Three info cards with icon, title, and description:
  1. When: "Every Wednesday" / "6:00 PM" — use a clock/calendar emoji or SVG icon
  2. Where: "Commons" / "Casablanca, Morocco" — use a location pin emoji or SVG icon
  3. Who: "200+ Members" / "Chess players & entrepreneurs" — use a people emoji or SVG icon
- Each card: bg-white rounded-xl shadow-sm border border-gray-100 p-6, hover:shadow-md transition
- Use simple inline SVG icons (no external icon library needed) or text emoji as fallback
- Section has light gray background (bg-gray-50) with generous padding

**components/CommunityStats.tsx:**
- Section explaining what C&C is in more detail
- Heading: "What is Checkmate & Connect?"
- 2-3 short paragraphs explaining: chess meets business networking, 2 years running, weekly consistency, Casablanca community
- Optional: a row of stat numbers (2+ years, 200+ members, weekly meetups) styled as large bold numbers with labels below
- Clean layout: max-w-3xl mx-auto for readable line lengths

**components/Footer.tsx:**
- Simple footer with: "Checkmate & Connect" name, copyright year (dynamic via new Date().getFullYear())
- Social/community links row: Meetup (placeholder URL), Instagram (placeholder URL)
- Subtle styling: bg-gray-900 text-gray-400, py-8 px-4
- Links use hover:text-white transition
  </action>
  <verify>Run `npx tsc --noEmit` to confirm all components compile. Verify each component file exists and exports a default or named function component.</verify>
  <done>Four landing page section components exist: Hero with community identity and CTA, EventDetails with when/where/who cards, CommunityStats with community description, Footer with links and copyright. All are Server Components, mobile-first responsive.</done>
</task>

<task type="auto">
  <name>Task 3: Compose landing page with sections and add JSON-LD structured data</name>
  <files>app/page.tsx</files>
  <action>
Replace the placeholder app/page.tsx with the complete landing page:

- Import all section components: Hero, EventDetails, CommunityStats, Footer
- Add JSON-LD structured data script tag for the recurring event:
  - @context: "https://schema.org"
  - @type: "Event"
  - name: "Checkmate & Connect Weekly Meetup"
  - description: "Join 200+ members for chess and entrepreneurship networking"
  - eventAttendanceMode: "https://schema.org/OfflineEventAttendanceMode"
  - eventStatus: "https://schema.org/EventScheduled"
  - location with @type Place, name "Commons", address with Casablanca, Morocco (MA)
  - organizer with @type Organization, name "Checkmate & Connect"
  - For startDate: compute next Wednesday at 18:00 Africa/Casablanca time zone (UTC+1) dynamically
  - Use dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }} on script tag

- Compose the page as:
  ```
  <Hero />
  <EventDetails />
  <CommunityStats />
  <Footer />
  ```

- This is a Server Component (no 'use client'). All data is static/hardcoded for now.
  </action>
  <verify>Run `npx tsc --noEmit` for type checking. Run `npm run build` to verify static generation works. Inspect the built HTML to confirm JSON-LD script tag is present.</verify>
  <done>Landing page composes Hero, EventDetails, CommunityStats, and Footer sections. JSON-LD Event structured data is embedded for search engine rich results. Page builds successfully as static HTML.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npm run build` — successful static build with no warnings
3. Landing page at / renders all four sections
4. View page source contains JSON-LD script with Event schema
5. View page source contains Open Graph meta tags
6. Page is responsive: readable on 375px width (mobile), proper grid on 1024px+ (desktop)
</verification>

<success_criteria>
- Landing page displays community identity (Checkmate & Connect, chess + entrepreneurship)
- Event details clearly show: every Wednesday, 6pm, Commons, Casablanca, Morocco
- Community stats/description provides context about the community
- Footer has community links and copyright
- SEO metadata includes Open Graph, Twitter cards, and JSON-LD Event schema
- Inter font loaded with zero layout shift via next/font
- All content mobile-first responsive with Tailwind CSS
- Static build succeeds (no dynamic data dependencies)
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-information/02-01-SUMMARY.md`
</output>
