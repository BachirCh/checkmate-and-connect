---
phase: 02-landing-information
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - components/MeetupWidget.tsx
  - app/page.tsx
autonomous: false

must_haves:
  truths:
    - "Visitor can see upcoming Meetup event via embedded widget"
    - "Meetup widget loads lazily without blocking initial page render"
    - "Landing page is visually complete and coherent on mobile and desktop"
  artifacts:
    - path: "components/MeetupWidget.tsx"
      provides: "Lazy-loaded Meetup iframe embed"
      contains: "use client"
    - path: "app/page.tsx"
      provides: "Landing page with MeetupWidget integrated"
      contains: "MeetupWidget"
  key_links:
    - from: "app/page.tsx"
      to: "components/MeetupWidget.tsx"
      via: "import"
      pattern: "import.*MeetupWidget"
---

<objective>
Add the Meetup widget embed to the landing page and verify the complete landing experience visually.

Purpose: The Meetup widget is the key call-to-action — it shows the next event and lets visitors RSVP directly. Visual verification ensures the full page works as the "send someone a link" experience.
Output: Complete landing page with Meetup embed, verified visually on mobile and desktop viewports.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-information/02-01-SUMMARY.md
@app/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MeetupWidget client component and integrate into landing page</name>
  <files>components/MeetupWidget.tsx, app/page.tsx</files>
  <action>
**components/MeetupWidget.tsx:**
- Mark as 'use client' (needs browser iframe rendering)
- Create a section with heading "Upcoming Events" (text-2xl font-bold)
- Embed Meetup widget via iframe:
  - src: "https://meeter.com/checkmate-connect" as placeholder (user will update with actual Meetup group URL)
  - Add a comment noting: "Replace URL with your Meetup group's embed URL from meetup.com/your-group"
  - loading="lazy" for deferred loading (critical for 3G performance)
  - width="100%" height="400"
  - title="Upcoming Checkmate & Connect Events" (accessibility)
  - className="border-0 rounded-lg"
- Wrap iframe in a container: max-w-3xl mx-auto
- Section padding: py-12 sm:py-16 px-4
- Add a fallback link below the iframe: "View all events on Meetup" linking to the Meetup group page, styled as a subtle text link (text-blue-600 hover:underline)
- This is the ONLY client component on the landing page — keep it minimal

**app/page.tsx:**
- Import MeetupWidget
- Insert MeetupWidget between CommunityStats and Footer in the page composition:
  ```
  <Hero />
  <EventDetails />
  <CommunityStats />
  <MeetupWidget />
  <Footer />
  ```
  </action>
  <verify>Run `npx tsc --noEmit` for type checking. Run `npm run build` to verify the page still builds with the client component boundary. Check that MeetupWidget is the only 'use client' component imported by the landing page.</verify>
  <done>MeetupWidget client component exists with lazy-loaded iframe, integrated into landing page between CommunityStats and Footer. Page builds successfully with minimal client JS.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete landing page visually</name>
  <files>app/page.tsx</files>
  <action>
Human verification of the complete landing page. What was built: Complete landing page with Hero (community identity + CTA), EventDetails (when/where/who cards), CommunityStats (community description), MeetupWidget (lazy iframe), Footer (links + copyright). SEO metadata, JSON-LD structured data, Inter font, mobile-first responsive design.

Steps to verify:
1. Run `npm run dev` and visit http://localhost:3000
2. Check desktop view (full browser width):
   - Hero section with "Checkmate & Connect" heading, subheading, and CTA button
   - Event details in 3-column grid (When/Where/Who)
   - Community description section
   - Meetup widget area (iframe may show placeholder if URL not configured)
   - Footer with links and copyright
3. Check mobile view (resize to ~375px or use DevTools device toolbar):
   - All sections stack vertically
   - Text is readable without horizontal scrolling
   - Cards stack in single column
   - Touch targets are large enough (buttons, links)
4. Right-click → View Page Source:
   - Confirm JSON-LD script tag with "Event" type is present
   - Confirm Open Graph meta tags are present
   - Confirm Inter font is loaded
5. Check that only the MeetupWidget section loads client-side JS (minimal bundle)

Resume: Type "approved" to complete Phase 2, or describe any visual/layout issues to fix.
  </action>
  <verify>User confirms visual approval of landing page on mobile and desktop viewports.</verify>
  <done>Landing page visually verified and approved by user as the "send someone a link" experience.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — zero TypeScript errors
2. `npm run build` — successful build
3. Landing page renders all 5 sections in order: Hero, EventDetails, CommunityStats, MeetupWidget, Footer
4. Meetup iframe has loading="lazy" attribute
5. Only one 'use client' component (MeetupWidget) on the entire page
6. User has visually approved the landing page
</verification>

<success_criteria>
- Meetup widget embedded with lazy loading for 3G performance
- Complete landing page visually verified by user on mobile and desktop
- Page answers "what's C&C?" with community identity, meeting details, and upcoming events
- Minimal client-side JavaScript (only MeetupWidget iframe)
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-information/02-02-SUMMARY.md`
</output>
