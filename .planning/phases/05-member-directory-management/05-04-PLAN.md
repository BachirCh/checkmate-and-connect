---
phase: 05-member-directory-management
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - app/admin/members/components/MemberRow.tsx
  - app/admin/members/components/EditMemberModal.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Admin can click Edit button on any member row"
    - "Admin can edit member name, job title, company, and LinkedIn fields"
    - "Changes save successfully and update both admin table and public directory"
  artifacts:
    - path: "app/admin/members/components/EditMemberModal.tsx"
      provides: "Modal form for editing member text fields"
      min_lines: 80
    - path: "app/admin/members/components/MemberRow.tsx"
      provides: "Edit button wired to editMember Server Action"
      contains: "editMember"
  key_links:
    - from: "app/admin/members/components/MemberRow.tsx"
      to: "app/actions/members.ts"
      via: "editMember Server Action with useActionState"
      pattern: "editMember"
    - from: "app/admin/members/components/EditMemberModal.tsx"
      to: "app/admin/members/components/MemberRow.tsx"
      via: "Modal state and form submission"
      pattern: "useState.*open"
---

<objective>
Close verification gap ADMIN-06 by adding Edit Member UI that connects to existing editMember Server Action.

Purpose: Enable admins to edit member text fields through UI instead of manual Sanity Studio edits
Output: Edit button, modal form, and wired Server Action completing member management workflow
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-member-directory-management/05-VERIFICATION.md
@.planning/phases/05-member-directory-management/05-02-SUMMARY.md
@app/actions/members.ts
@app/admin/members/components/MemberRow.tsx
</context>

<gap_context>
**Gap from VERIFICATION.md:**
Truth "Admin can edit existing member profiles" FAILED because editMember Server Action exists (lines 118-148 in app/actions/members.ts) but no UI to trigger it.

**What exists:**
- Server Action: editMember(memberId, updates) with verifySession() ✓
- Authorization: Proper admin check ✓
- Revalidation: Updates both /admin/members and /members paths ✓
- Type signature: Accepts name, jobTitle, company, linkedIn fields ✓

**What's missing:**
- Edit button in MemberRow component
- Edit modal or inline form for member fields
- Form wiring using useActionState pattern (established in Plan 05-02)
</gap_context>

<tasks>

<task type="auto">
  <name>Task 1: Create EditMemberModal component with form fields</name>
  <files>app/admin/members/components/EditMemberModal.tsx</files>
  <action>
Create new Client Component EditMemberModal.tsx with controlled form for editing member text fields.

**Component signature:**
```tsx
'use client';
import { useActionState, useState, useEffect } from 'react';
import { editMember } from '@/app/actions/members';

type EditMemberModalProps = {
  member: {
    _id: string;
    name: string;
    jobTitle: string;
    company?: string;
    linkedIn?: string;
  };
  open: boolean;
  onClose: () => void;
};
```

**Implementation requirements:**
1. Modal overlay with dark backdrop (bg-black/50) and centered white card on black bg
2. Form fields for name (required), jobTitle (required), company (optional), linkedIn (optional)
3. Use useActionState to wire editMember Server Action with memberId
4. Local state for form fields initialized from member prop
5. Reset form fields when modal opens (useEffect with open dependency)
6. Submit button shows "Saving..." during pending state
7. Display error message if editState.error exists
8. Close modal on successful submission (editState.success === true)
9. Cancel button to close without submitting
10. Follow existing MemberRow patterns: gray-800 borders, white text on black bg, hover states

**Key patterns from Plan 05-02:**
- useActionState(async () => await editMember(memberId, formData), null)
- Disabled state during pending with opacity-50
- Error display with text-red-400
- Tailwind classes matching existing admin UI (black/white/gray-800 theme)
  </action>
  <verify>
File exists at app/admin/members/components/EditMemberModal.tsx
Contains useActionState import
Contains editMember import from @/app/actions/members
Contains form fields for name, jobTitle, company, linkedIn
Contains modal overlay and centering styles
  </verify>
  <done>
EditMemberModal component created with controlled form fields, useActionState wiring, and modal UI matching existing admin theme
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Edit button and modal state to MemberRow</name>
  <files>app/admin/members/components/MemberRow.tsx</files>
  <action>
Update MemberRow.tsx to add Edit button and EditMemberModal integration.

**Changes required:**
1. Import EditMemberModal component at top
2. Add local state: `const [editOpen, setEditOpen] = useState(false);`
3. Add Edit button alongside existing action buttons (Approve/Reject/Delete)
4. Render EditMemberModal component with props:
   - member={member}
   - open={editOpen}
   - onClose={() => setEditOpen(false)}

**Edit button placement:**
- Add BEFORE the Delete button in the actions cell
- Show for ALL statuses (pending, approved, rejected) - not conditional
- Style: `px-3 py-1 text-sm bg-blue-600 text-white rounded hover:bg-blue-700 transition-colors`
- Button text: "Edit"
- onClick handler: `onClick={() => setEditOpen(true)}`

**Modal rendering:**
Place EditMemberModal at the end of the component JSX (after the closing tr tag), outside the table row structure to avoid DOM nesting issues.

Follow existing code style and patterns from lines 25-36 (useActionState for other actions).
  </action>
  <verify>
MemberRow.tsx imports EditMemberModal
Contains useState for editOpen
Contains Edit button with onClick handler
Contains EditMemberModal component with proper props
Edit button appears in UI alongside other action buttons
  </verify>
  <done>
Edit button added to MemberRow with modal state management, EditMemberModal rendered conditionally based on editOpen state
  </done>
</task>

<task type="auto">
  <name>Task 3: Test edit workflow end-to-end</name>
  <files>app/admin/members/components/EditMemberModal.tsx</files>
  <action>
Verify the complete edit workflow by simulating the interaction flow.

**Manual verification steps documented (for human testing later):**
1. Login to /admin/members
2. Click Edit button on any member row
3. Modal opens with pre-filled member data
4. Change one or more fields (name, jobTitle, company, linkedIn)
5. Click Save button
6. Modal shows "Saving..." state
7. Modal closes on success
8. Admin table refreshes with updated data
9. Visit /members to confirm changes appear in public directory (if approved)

**Code verification (automated):**
- Verify EditMemberModal imports and uses editMember correctly
- Verify form submission passes memberId and updates object
- Verify modal closes on editState.success === true
- Verify error handling displays editState.error
- Add console.log for debugging if needed (remove before commit)

Gap closure reason: editMember Server Action exists but had no UI trigger (VERIFICATION.md gap).
  </action>
  <verify>
Build succeeds: `npm run build`
No TypeScript errors
EditMemberModal component exports default
MemberRow imports EditMemberModal without errors
  </verify>
  <done>
Edit workflow tested via build verification, ready for human verification checkpoint in Phase 5 completion
  </done>
</task>

</tasks>

<verification>
**Gap Closure Verification:**

Run gap verification check:
```bash
# 1. Verify Edit button exists in MemberRow
grep -n "Edit" app/admin/members/components/MemberRow.tsx

# 2. Verify EditMemberModal component exists
ls -la app/admin/members/components/EditMemberModal.tsx

# 3. Verify editMember import in MemberRow
grep "editMember" app/admin/members/components/MemberRow.tsx

# 4. Build check
npm run build
```

Expected outcomes:
- MemberRow contains "Edit" button ✓
- EditMemberModal.tsx file exists ✓
- MemberRow does NOT directly import editMember (modal handles it) ✓
- Build succeeds without errors ✓

**Truth verification:**
- Truth: "Admin can click Edit button on any member row" → Edit button visible in UI ✓
- Truth: "Admin can edit member fields" → Modal form with all text fields ✓
- Truth: "Changes save and update both paths" → editMember Server Action called via useActionState ✓
</verification>

<success_criteria>
1. EditMemberModal component exists with controlled form fields
2. MemberRow has Edit button and modal state management
3. Edit button triggers modal open on click
4. Modal form submits to editMember Server Action via useActionState
5. Modal closes on successful submission
6. Build completes without TypeScript or lint errors
7. Gap ADMIN-06 from VERIFICATION.md is closed
</success_criteria>

<output>
After completion, create `.planning/phases/05-member-directory-management/05-04-SUMMARY.md`
</output>
