---
phase: 05-member-directory-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/(public)/members/page.tsx
  - components/MemberCard.tsx
  - components/MemberGrid.tsx
  - components/MemberHighlights.tsx
autonomous: true

must_haves:
  truths:
    - "Visitor can browse approved members in public directory at /members"
    - "Member cards display photo, name, job title, company, and LinkedIn link"
    - "Photos use Sanity CDN with automatic format optimization (WebP/AVIF)"
    - "Directory shows accurate count of approved members"
    - "Grid layout is responsive (2 cols mobile, 3 cols tablet, 4 cols desktop)"
    - "Grayscale-to-color hover effect works on member photos"
  artifacts:
    - path: "app/(public)/members/page.tsx"
      provides: "Server Component fetching approved members with GROQ query"
      min_lines: 40
      exports: ["default", "metadata"]
    - path: "components/MemberCard.tsx"
      provides: "Single member card with image and hover overlay"
      min_lines: 50
      contains: "urlFor(member.photo)"
    - path: "components/MemberGrid.tsx"
      provides: "Responsive grid container for member cards"
      min_lines: 15
      contains: "grid-cols-2 md:grid-cols-3 lg:grid-cols-4"
  key_links:
    - from: "app/(public)/members/page.tsx"
      to: "Sanity CMS"
      via: "GROQ query filtering status=='approved'"
      pattern: "status == \"approved\""
    - from: "components/MemberCard.tsx"
      to: "Sanity CDN"
      via: "urlFor() image builder"
      pattern: "urlFor\\(.*\\).width\\(.*\\).url\\(\\)"
    - from: "components/MemberGrid.tsx"
      to: "components/MemberCard.tsx"
      via: "renders array of MemberCard components"
      pattern: "MemberCard.*member="
---

<objective>
Build public member directory page where visitors can browse all approved members with photos, professional information, and LinkedIn links using Sanity CDN image optimization and responsive grid layout.

Purpose: Fulfill DIR-01, DIR-02, DIR-03, DIR-04, DIR-05 requirements for public member visibility
Output: Functional `/members` route with optimized member cards and updated home page highlights
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-member-directory-management/05-RESEARCH.md

# Phase 1 foundation
@lib/sanity/client.ts
@lib/sanity/imageUrl.ts
@lib/sanity/schemas/member.ts

# Phase 2 visual patterns
@components/MemberHighlights.tsx
@app/(public)/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create public member directory page</name>
  <files>
    app/(public)/members/page.tsx
  </files>
  <action>
Create Server Component at `app/(public)/members/page.tsx` that fetches approved members from Sanity and displays them in a responsive grid.

**Implementation:**
- Import client from `@/lib/sanity/client` (NOT writeClient - read-only)
- Define GROQ query: `*[_type == "member" && status == "approved"] | order(approvedAt desc) { _id, name, slug, photo, jobTitle, company, linkedIn, approvedAt }`
- CRITICAL: ALWAYS include `status == "approved"` filter for privacy compliance (DIR-04)
- Fetch members in Server Component using `await client.fetch(query)`
- Add metadata export with title and description for SEO
- Render layout with centered heading, member count display, and MemberGrid component
- Use existing black/white theme: `bg-black text-white`, `py-16` spacing
- Handle empty state: "No approved members yet. Check back soon!"
- Member count format: "{count} approved members" in gray-400

**Why Server Component (not client-side):**
- Eliminates loading states and reduces bundle size
- Members indexed by search engines for SEO
- Sanity CDN caching provides fast response times

**Pattern reference:** Follow structure from `app/(public)/page.tsx` landing page
  </action>
  <verify>
Run `npm run dev`, visit http://localhost:3000/members, verify page renders with heading and member grid (may be empty if no approved members exist yet)

Check page source contains metadata: `<title>Member Directory | Checkmate & Connect</title>`
  </verify>
  <done>
- Server Component successfully fetches approved members using GROQ query with status filter
- Page renders with heading, member count, and MemberGrid component
- Metadata export exists for SEO
- Empty state message displays when no approved members exist
  </done>
</task>

<task type="auto">
  <name>Task 2: Build responsive member card components with Sanity CDN</name>
  <files>
    components/MemberCard.tsx
    components/MemberGrid.tsx
  </files>
  <action>
Create two components for displaying member profiles with optimized images and responsive layout.

**MemberCard.tsx:**
- Create component accepting `member` prop with type: `{ _id: string, name: string, slug: { current: string }, photo: any, jobTitle: string, company?: string, linkedIn?: string }`
- Import `urlFor` from `@/lib/sanity/imageUrl`
- Generate optimized image URL: `urlFor(member.photo).width(400).height(400).fit('crop').auto('format').url()`
  - `.fit('crop')` uses hotspot for focal point (already enabled in schema)
  - `.auto('format')` serves WebP/AVIF automatically to supporting browsers
- Render card structure (extend existing MemberHighlights pattern):
  - Outer div: `group relative overflow-hidden rounded-xl aspect-square bg-gray-900 border border-gray-800 hover:border-gray-700 transition-all`
  - Image: `grayscale group-hover:grayscale-0 transition-all duration-300` (existing hover effect)
  - Hover overlay: `absolute inset-0 bg-gradient-to-t from-black via-black/50 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300`
  - Info overlay (bottom): name (white, font-semibold, text-sm), jobTitle (gray-300, text-xs), company (gray-400, text-xs), LinkedIn link (blue-400, text-xs, hover:underline)
- LinkedIn link: `target="_blank" rel="noopener noreferrer"` for security
- LinkedIn label: "LinkedIn →" with arrow for clarity

**MemberGrid.tsx:**
- Create component accepting `members` array prop
- Render responsive grid: `grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6`
- Map over members, render MemberCard for each with `key={member._id}`
- Export default for use in directory page

**Mobile-first design:** 2 columns on mobile, 3 on tablet (md:), 4 on desktop (lg:)
  </action>
  <verify>
Check files exist:
- `components/MemberCard.tsx` contains urlFor() with .width(400).height(400).fit('crop').auto('format')
- `components/MemberGrid.tsx` contains responsive grid classes

Run `npm run dev`, visit /members, inspect Network tab to verify Sanity CDN image URLs include format=webp or format=avif (if browser supports)
  </verify>
  <done>
- MemberCard component uses Sanity urlFor() with optimization parameters
- Grayscale-to-color hover effect works on images
- Card displays all member fields (name, title, company, LinkedIn) in hover overlay
- MemberGrid uses responsive grid layout (2/3/4 columns)
- LinkedIn links open in new tab with security attributes
  </done>
</task>

<task type="auto">
  <name>Task 3: Update home page highlights to use real Sanity data</name>
  <files>
    components/MemberHighlights.tsx
  </files>
  <action>
Replace placeholder data in MemberHighlights component with real approved members from Sanity.

**Convert to Server Component:**
- Remove placeholder `recentMembers` array (currently hardcoded Unsplash images)
- Import `client` from `@/lib/sanity/client`
- Import `urlFor` from `@/lib/sanity/imageUrl`
- Declare component as async: `export default async function MemberHighlights()`
- Fetch recent approved members: `await client.fetch('*[_type == "member" && status == "approved"] | order(approvedAt desc) [0...8] { _id, name, slug, photo, jobTitle }')`
  - CRITICAL: Include `status == "approved"` filter
  - Limit to 8 most recent using `[0...8]` GROQ slice syntax
- Generate image URLs: `urlFor(member.photo).width(400).height(400).fit('crop').auto('format').url()`
- Update map to use `member._id` as key (not hardcoded id)
- Display member.name and member.jobTitle (replace placeholder role field)
- Keep existing grid layout: `grid-cols-2 md:grid-cols-4`
- Keep "See All Members" link to `/members`

**Maintain existing visual design:**
- Black background with gray-800 borders
- Grayscale hover effect
- Gradient overlay on hover

**Handle empty state:** If no approved members, render message: "Our member directory will launch soon!" (centered, gray-400)
  </action>
  <verify>
Run `npm run dev`, visit http://localhost:3000 (home page), verify:
1. Member cards show real data from Sanity (not placeholder names)
2. Photos load from Sanity CDN (check Network tab for cdn.sanity.io URLs)
3. Hover effect still works (grayscale to color)
4. "See All Members" button links to /members

If no approved members exist, verify empty state message displays
  </verify>
  <done>
- MemberHighlights fetches real approved members from Sanity using GROQ query
- Component uses Sanity CDN for optimized images with hotspot support
- Displays up to 8 most recent approved members
- Empty state handles zero approved members gracefully
- Existing visual design and hover effects preserved
  </done>
</task>

</tasks>

<verification>
**Directory completeness:**
1. Visit `/members` - page loads without errors
2. Member count is accurate (matches approved count in Sanity Studio)
3. All member cards display photos from Sanity CDN (not broken images)
4. Member information complete: name, title, company (if present), LinkedIn link
5. Click LinkedIn link - opens in new tab to correct profile

**Responsive design:**
1. Resize browser window: verify 2 cols (mobile), 3 cols (tablet), 4 cols (desktop)
2. Test on mobile device if available

**Image optimization:**
1. Open Network tab, filter by images
2. Verify Sanity CDN URLs include `auto=format` parameter
3. Check image format is WebP or AVIF (if browser supports)
4. Verify images load quickly (CDN caching working)

**Privacy compliance:**
1. Verify GROQ queries include `status == "approved"` filter
2. Check that pending members do NOT appear in directory (confirm in Sanity Studio)
3. Member count matches approved members only (not total members)

**Home page integration:**
1. Visit home page - MemberHighlights shows real member data
2. Click "See All Members" - navigates to `/members`
</verification>

<success_criteria>
- Public member directory page exists at `/members` route with SEO metadata
- Visitor can see all approved members with photos and professional information (DIR-01, DIR-02)
- Member profiles display name, photo, job title, company (optional), and LinkedIn link (DIR-03)
- Directory page is mobile-responsive with 2/3/4 column grid layout (DIR-04, DIR-05)
- Directory includes accurate member count of approved members (DIR-05)
- Photos use Sanity CDN with automatic format optimization and hotspot support
- GROQ queries always filter by `status == "approved"` for privacy compliance
- Home page MemberHighlights component uses real Sanity data (no placeholders)
- Grayscale-to-color hover effect works consistently across all member cards
</success_criteria>

<output>
After completion, create `.planning/phases/05-member-directory-management/05-01-SUMMARY.md`
</output>
