---
phase: 06-launch-preparation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lighthouserc.json
  - scripts/test-performance.sh
  - scripts/test-rate-limits.js
  - .github/workflows/lighthouse-ci.yml
  - docs/device-testing-checklist.md
autonomous: false

must_haves:
  truths:
    - "All pages load within 3 seconds on mobile 3G"
    - "Core Web Vitals are green (LCP < 2.5s, INP < 200ms, CLS < 0.1)"
    - "Site tested on at least 3 physical mobile devices (iOS and Android)"
    - "Rate limiting blocks spam after 5 rapid submissions"
    - "Performance regression testing automated in CI/CD"
  artifacts:
    - path: "lighthouserc.json"
      provides: "Performance budgets configuration"
      contains: "budgets"
    - path: ".github/workflows/lighthouse-ci.yml"
      provides: "Automated Lighthouse testing workflow"
      contains: "treosh/lighthouse-ci-action"
    - path: "scripts/test-rate-limits.js"
      provides: "k6 load test for spam protection"
      contains: "http.post"
    - path: "docs/device-testing-checklist.md"
      provides: "Physical device testing protocol"
      min_lines: 50
  key_links:
    - from: ".github/workflows/lighthouse-ci.yml"
      to: "lighthouserc.json"
      via: "budgetPath configuration"
      pattern: "budgetPath.*lighthouserc"
    - from: "scripts/test-rate-limits.js"
      to: "lib/spam-protection.ts"
      via: "POST requests to test rateLimit function"
      pattern: "http.post.*api"
---

<objective>
Establish automated performance testing, validate Core Web Vitals across devices, and verify spam protection under load.

Purpose: Ensure production readiness through systematic performance benchmarking and real-world device testing. Prevent performance regressions via CI/CD automation.

Output: Lighthouse CI workflow, performance budgets, k6 rate limit tests, and physical device testing protocol with verified green Core Web Vitals across all key pages.
</objective>

<execution_context>
@/Users/mac/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mac/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-launch-preparation/06-RESEARCH.md
@.planning/phases/01-foundation-infrastructure/01-04-SUMMARY.md

# Existing infrastructure from Phase 1
@netlify.toml
@lib/spam-protection.ts
@app/robots.ts
@app/sitemap.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure Lighthouse CI and performance budgets</name>
  <files>
    lighthouserc.json
    .github/workflows/lighthouse-ci.yml
    scripts/test-performance.sh
  </files>
  <action>
Create Lighthouse CI configuration with performance budgets aligned to Core Web Vitals requirements (LCP < 2.5s, INP < 200ms, CLS < 0.1).

**lighthouserc.json:**
- Create performance budgets for homepage, /members, and /admin paths
- Set timing budgets: interactive < 3000ms, first-contentful-paint < 1500ms
- Set resource size budgets: script < 300KB, total < 1000KB
- Include budget for all Core Web Vitals metrics

**.github/workflows/lighthouse-ci.yml:**
- Create GitHub Actions workflow triggered on pull requests
- Use treosh/lighthouse-ci-action@v11 for automated testing
- Test staging deployment URLs (https://staging.checkmate-connect.com or Netlify preview URL)
- Upload artifacts and use temporary public storage for reports
- Reference lighthouserc.json for budget enforcement

**scripts/test-performance.sh:**
- Create local performance testing script using Lighthouse CLI
- Test homepage, /members, /admin with Fast 3G throttling
- Generate HTML reports in performance-reports/ directory
- Include instructions for running locally: `bash scripts/test-performance.sh`

Use research patterns from 06-RESEARCH.md Pattern 1 (Lighthouse CI with Performance Budgets).

NOTE: Do not install Lighthouse packages yet - focus on configuration. Installation happens in verification step.
  </action>
  <verify>
1. Verify lighthouserc.json exists with budgets object
2. Verify .github/workflows/lighthouse-ci.yml uses treosh/lighthouse-ci-action@v11
3. Verify scripts/test-performance.sh is executable (chmod +x)
4. Run `cat lighthouserc.json | grep -E "(interactive|first-contentful-paint|budgets)"` to confirm budget structure
  </verify>
  <done>
lighthouserc.json defines performance budgets for key pages, GitHub Actions workflow configured for automated Lighthouse testing on PRs, and local testing script ready for manual performance audits.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create k6 rate limit load test</name>
  <files>
    scripts/test-rate-limits.js
    package.json
  </files>
  <action>
Create k6 load testing script to verify spam protection (rate limiting, honeypot, reCAPTCHA) holds under simulated bot attacks.

**scripts/test-rate-limits.js:**
- Import k6 http, check, sleep modules
- Configure test stages: 30s ramp to 20 users, 1min sustain, 10s ramp down
- Set thresholds: 95% of requests under 500ms, >80% should hit rate limit (status 429)
- Create POST request to form endpoint (member submission or contact form when available)
- Include realistic form payload with name, email, message
- Include honeypot field (_honey: '') to test legitimate user path
- Test rapid-fire submissions (10 requests/second per user)
- Use check() to verify status is 200 or 429 (not server errors)
- Add sleep(0.1) between requests to simulate bot behavior

**package.json:**
- Add k6 installation note in scripts section: `"test:load": "k6 run scripts/test-rate-limits.js"`
- Do NOT add k6 as npm dependency (installed globally or via homebrew)

Use research patterns from 06-RESEARCH.md Pattern 2 (k6 Rate Limit Testing).

Target endpoint: Will need actual form endpoint from Phase 3 (member submission). For now, target /api/health as placeholder with comment noting form endpoint substitution needed.
  </action>
  <verify>
1. Verify scripts/test-rate-limits.js exists with k6 imports
2. Check file includes options.stages and options.thresholds configuration
3. Verify POST request includes honeypot field and form payload
4. Run `grep -E "(http.post|check|thresholds)" scripts/test-rate-limits.js` to confirm k6 patterns
5. Verify package.json scripts section includes test:load command
  </verify>
  <done>
k6 load test script ready to simulate spam attacks, validates rate limiting triggers after threshold, and package.json documents load testing command.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create physical device testing checklist</name>
  <files>
    docs/device-testing-checklist.md
  </files>
  <action>
Create comprehensive physical device testing protocol documenting manual verification steps for iOS and Android devices.

**docs/device-testing-checklist.md:**

Structure:
1. **Test Devices** section - Minimum 3 devices: iPhone (latest iOS + Safari), Android (latest + Chrome), Android/iPhone (2-3 years old + default browser)
2. **Per-Device Test Cases** organized by page:
   - Homepage: Load time, images, navigation, CTAs, no horizontal scroll, readable text
   - Member Directory: List rendering, scrolling performance, images, profile links
   - Admin pages: Login form, dashboard, member management actions
   - Forms (when available): Input accessibility, keyboard behavior, reCAPTCHA badge, submission flow
3. **Performance Checks**: Lighthouse mobile score >90, CLS < 0.1, LCP < 2.5s, smooth scrolling
4. **Tools Section**: Chrome DevTools Remote Debugging (Android), Safari Web Inspector (iOS), Network throttling to Fast 3G

Include checkboxes for each test case to track completion across devices.

Add warning about limitations: "Chrome DevTools mobile emulation cannot replicate real device GPUs, touch behavior, and performance. Always test on physical devices before launch."

Use research patterns from 06-RESEARCH.md Pattern 5 (Physical Device Testing Protocol).
  </action>
  <verify>
1. Verify docs/device-testing-checklist.md exists
2. Check file includes minimum 3 device requirement
3. Verify checklist has sections for Homepage, Member Directory, Admin, Forms, Performance
4. Run `wc -l docs/device-testing-checklist.md` and confirm >50 lines
5. Verify checkboxes (- [ ]) exist for test cases
  </verify>
  <done>
Physical device testing checklist documents systematic testing protocol for iOS and Android devices with specific test cases per page and performance criteria.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify performance testing infrastructure and device testing</name>
  <action>
Human verification of automated performance testing infrastructure (Lighthouse CI workflow, k6 load tests) and physical device testing protocol ready for launch validation.
  </action>
  <what-built>
Automated performance testing infrastructure (Lighthouse CI workflow, k6 load tests) and physical device testing protocol ready for launch validation.
  </what-built>
  <how-to-verify>
**1. Install testing tools:**
```bash
# Install Lighthouse CLI for local testing
npm install --save-dev @lhci/cli lighthouse chrome-launcher

# Install k6 for load testing (macOS)
brew install k6
# OR download from: https://github.com/grafana/k6/releases
```

**2. Run local performance test:**
```bash
bash scripts/test-performance.sh
# Expected: Generates HTML reports in performance-reports/ directory
# Open reports to verify Core Web Vitals scores
```

**3. Test rate limiting (requires form endpoint from Phase 3):**
```bash
# NOTE: Will need actual form endpoint, currently uses /api/health placeholder
k6 run scripts/test-rate-limits.js
# Expected: Shows >80% requests hit rate limit (429 status)
```

**4. Physical device testing:**
- Open docs/device-testing-checklist.md
- Follow checklist on at least 3 physical devices:
  - Test homepage load time on 3G
  - Verify navigation works on mobile
  - Check member directory scrolling
  - Test admin login on mobile Safari and Chrome
- Check off completed items in checklist

**5. Review Lighthouse CI configuration:**
- Verify lighthouserc.json budgets are realistic for current site
- Confirm GitHub Actions workflow will run on PRs

**Success criteria:**
- [ ] Local Lighthouse reports show green Core Web Vitals (90+ scores)
- [ ] k6 load test confirms rate limiting works (or note form endpoint needed)
- [ ] Physical device testing shows no critical issues on 3+ devices
- [ ] All automated testing configurations are correct

**Issues found:** Document any performance problems or device-specific bugs for resolution before launch.
  </how-to-verify>
  <verify>
Run automated verification scripts and complete physical device testing protocol per instructions in how-to-verify section.
  </verify>
  <done>
All automated testing infrastructure validated, physical device testing completed on 3+ devices with no critical issues, and all Core Web Vitals requirements met.
  </done>
  <resume-signal>
Type "approved" if all testing infrastructure works and device testing shows no blockers, OR describe issues that need fixing before proceeding.
  </resume-signal>
</task>

</tasks>

<verification>
## Performance Testing
- [ ] lighthouserc.json defines budgets for all key pages
- [ ] GitHub Actions workflow configured for automated PR testing
- [ ] Local performance testing script generates HTML reports
- [ ] k6 load test script validates rate limiting under load

## Physical Device Testing
- [ ] Testing checklist covers minimum 3 devices (iOS + Android)
- [ ] Checklist includes all key pages and performance criteria
- [ ] Manual testing completed on actual devices (confirmed in checkpoint)

## Core Web Vitals
- [ ] LCP (Largest Contentful Paint) < 2.5 seconds on mobile 3G
- [ ] INP (Interaction to Next Paint) < 200 milliseconds
- [ ] CLS (Cumulative Layout Shift) < 0.1
- [ ] Lighthouse mobile score > 90 for all key pages

## Spam Protection
- [ ] Rate limiting triggers after 5 rapid submissions
- [ ] k6 tests confirm >80% requests hit rate limit under load
- [ ] Honeypot and reCAPTCHA verified in testing
</verification>

<success_criteria>
1. Lighthouse CI workflow runs automatically on pull requests and enforces performance budgets
2. Local performance testing script generates detailed HTML reports for all key pages
3. k6 load test confirms rate limiting blocks spam attempts (>80% hit 429 status)
4. Physical device testing checklist completed on 3+ devices with no critical issues
5. All pages achieve green Core Web Vitals (LCP < 2.5s, INP < 200ms, CLS < 0.1) on mobile
6. Automated testing infrastructure prevents performance regressions in future development
</success_criteria>

<output>
After completion, create `.planning/phases/06-launch-preparation/06-01-SUMMARY.md` documenting:
- Performance baseline metrics (Lighthouse scores, Core Web Vitals)
- Device testing results (devices tested, issues found)
- Rate limit testing results (k6 output showing spam protection works)
- Any performance optimizations needed before launch
</output>
